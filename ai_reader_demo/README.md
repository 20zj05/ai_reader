# AI 伴读助手（Qwen 版）

基于阿里云 **通义千问（Qwen）** 的微信读书 AI 阅读功能 Demo。

## 🚀 快速开始

1. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
2.api-key说明：
   针对api-key路径文件[.env](.env)
   ```
   因隐私安全等考虑，此处api-key需使用者自行申请，该文件中使用API-KEY-EXAMPLE代替
   ```